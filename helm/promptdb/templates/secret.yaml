apiVersion: v1
kind: Secret
metadata:
  name: {{ include "promptdb.fullname" . }}-secret
  labels:
    {{- include "promptdb.labels" . | nindent 4 }}
type: Opaque
stringData:
  secretKey: {{ .Values.secret.secretKey | quote }}
